# frontend

UI Next.js di **port 3000**.

- `app/` — halaman (`/`, `/jobs/[id]`, `/voice`)
- `components/` — form, kartu clip, header
- `lib/` — fetch `/api/...` (di-proxy ke backend :4000)
- `styles/` — CSS
